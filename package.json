{
  "name": "diffsmith",
  "displayName": "DiffSmith",
  "icon": "images/DiffSmith_logo.png",
  "description": "Generate git commit messages using AI (NVIDIA first, multi-provider ready).",
  "version": "0.1.0",
  "publisher": "SupunLiyanage",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/SupunLiyanage88/CommitForge.git"
  },
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "SCM Providers",
    "Other"
  ],
  "activationEvents": [
    "onCommand:diffsmith.generateCommit",
    "onCommand:diffsmith.configureProvider"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "diffsmith.generateCommit",
        "title": "DiffSmith: Generate Commit Message",
        "icon": "$(sparkle)"
      },
      {
        "command": "diffsmith.configureProvider",
        "title": "DiffSmith: Configure Provider"
      }
    ],
    "menus": {
      "commandPalette": [
        {
          "command": "diffsmith.generateCommit",
          "when": "gitOpenRepositoryCount >= 1"
        },
        {
          "command": "diffsmith.configureProvider"
        }
      ],
      "scm/title": [
        {
          "command": "diffsmith.generateCommit",
          "group": "navigation",
          "when": "scmProvider == git"
        }
      ]
    },
    "configuration": {
      "title": "DiffSmith",
      "properties": {
        "diffsmith.provider": {
          "type": "string",
          "enum": [
            "nvidia",
            "openrouter"
          ],
          "default": "nvidia",
          "description": "AI provider used to generate commit messages."
        },
        "diffsmith.model": {
          "type": "string",
          "default": "deepseek-ai/deepseek-v4-pro-0813",
          "description": "Model ID for the active provider. Change it via DiffSmith: Configure Provider, which shows rated recommendations."
        },
        "diffsmith.commitStyle": {
          "type": "string",
          "enum": [
            "conventional",
            "simple",
            "gitmoji",
            "custom"
          ],
          "default": "conventional",
          "description": "Commit message style."
        },
        "diffsmith.maxDiffSize": {
          "type": "number",
          "default": 30000,
          "description": "Maximum diff size in characters before per-file truncation kicks in."
        },
        "diffsmith.customInstructions": {
          "type": "string",
          "default": "",
          "description": "Custom instructions appended to the prompt when commitStyle is 'custom' (or extra guidance for any style)."
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "pretest": "npm run compile",
    "lint": "eslint src --ext ts"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.6",
    "@types/node": "^20.11.0",
    "@types/vscode": "^1.85.0",
    "eslint": "^8.57.0",
    "typescript": "^5.4.0"
  }
}
