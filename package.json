{
  "name": "diffly",
  "displayName": "Diffly",
  "icon": "images/Diffly_logo.png",
  "description": "Generate git commit messages using AI (NVIDIA first, multi-provider ready).",
  "version": "0.1.0",
  "publisher": "SupunLiyanage",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/SupunLiyanage88/CommitForge.git"
  },
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "SCM Providers",
    "Other"
  ],
  "activationEvents": [
    "onCommand:diffly.generateCommit",
    "onCommand:diffly.configureProvider"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "diffly.generateCommit",
        "title": "Diffly: Generate Commit Message",
        "icon": {
          "light": "images/Diffly_logo.png",
          "dark": "images/Diffly_logo.png"
        }
      },
      {
        "command": "diffly.configureProvider",
        "title": "Diffly: Configure Provider"
      }
    ],
    "menus": {
      "commandPalette": [
        {
          "command": "diffly.generateCommit",
          "when": "gitOpenRepositoryCount >= 1"
        },
        {
          "command": "diffly.configureProvider"
        }
      ],
      "scm/title": [
        {
          "command": "diffly.generateCommit",
          "group": "navigation",
          "when": "scmProvider == git"
        }
      ]
    },
    "configuration": {
      "title": "Diffly",
      "properties": {
        "diffly.provider": {
          "type": "string",
          "enum": [
            "nvidia",
            "openrouter"
          ],
          "default": "nvidia",
          "description": "AI provider used to generate commit messages."
        },
        "diffly.model": {
          "type": "string",
          "default": "deepseek-ai/deepseek-v4-pro-0813",
          "description": "Model ID for the active provider. Change it via Diffly: Configure Provider, which shows rated recommendations."
        },
        "diffly.commitStyle": {
          "type": "string",
          "enum": [
            "conventional",
            "simple",
            "gitmoji",
            "custom"
          ],
          "default": "conventional",
          "description": "Commit message style."
        },
        "diffly.maxDiffSize": {
          "type": "number",
          "default": 30000,
          "description": "Maximum diff size in characters before per-file truncation kicks in."
        },
        "diffly.customInstructions": {
          "type": "string",
          "default": "",
          "description": "Custom instructions appended to the prompt when commitStyle is 'custom' (or extra guidance for any style)."
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "pretest": "npm run compile",
    "lint": "eslint src --ext ts"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.6",
    "@types/node": "^20.11.0",
    "@types/vscode": "^1.85.0",
    "eslint": "^8.57.0",
    "typescript": "^5.4.0"
  }
}
