{
  "name": "commitloom",
  "displayName": "CommitLoom",
  "icon": "images/CommitLoom_logo.png",
  "description": "Generate meaningful git commit messages with AI — NVIDIA & OpenRouter, one click from Source Control.",
  "version": "0.1.0",
  "publisher": "SupunLiyanage",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/SupunLiyanage88/CommitForge.git"
  },
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "SCM Providers",
    "Other"
  ],
  "activationEvents": [
    "onCommand:commitloom.generateCommit",
    "onCommand:commitloom.configureProvider"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "commitloom.generateCommit",
        "title": "CommitLoom: Generate Commit Message",
        "icon": {
          "light": "images/icon_v2.png",
          "dark": "images/icon_v2-dark.png"
        }
      },
      {
        "command": "commitloom.configureProvider",
        "title": "CommitLoom: Configure Provider"
      }
    ],
    "menus": {
      "commandPalette": [
        {
          "command": "commitloom.generateCommit",
          "when": "gitOpenRepositoryCount >= 1"
        },
        {
          "command": "commitloom.configureProvider"
        }
      ],
      "scm/title": [
        {
          "command": "commitloom.generateCommit",
          "group": "navigation",
          "when": "scmProvider == git"
        }
      ]
    },
    "configuration": {
      "title": "CommitLoom",
      "properties": {
        "commitloom.provider": {
          "type": "string",
          "enum": [
            "nvidia",
            "openrouter"
          ],
          "default": "nvidia",
          "description": "AI provider used to generate commit messages."
        },
        "commitloom.model": {
          "type": "string",
          "default": "deepseek-ai/deepseek-v4-pro-0813",
          "description": "Model ID for the active provider. Change it via CommitLoom: Configure Provider, which shows rated recommendations."
        },
        "commitloom.commitStyle": {
          "type": "string",
          "enum": [
            "conventional",
            "simple",
            "gitmoji",
            "custom"
          ],
          "default": "conventional",
          "description": "Commit message style."
        },
        "commitloom.maxDiffSize": {
          "type": "number",
          "default": 30000,
          "description": "Maximum diff size in characters before per-file truncation kicks in."
        },
        "commitloom.customInstructions": {
          "type": "string",
          "default": "",
          "description": "Custom instructions appended to the prompt when commitStyle is 'custom' (or extra guidance for any style)."
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "pretest": "npm run compile",
    "lint": "eslint src --ext ts"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.6",
    "@types/node": "^20.11.0",
    "@types/vscode": "^1.85.0",
    "eslint": "^8.57.0",
    "typescript": "^5.4.0"
  }
}
